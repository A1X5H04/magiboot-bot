{
  "tasks": {
    "dev": "deno run -IEN --watch --env main.ts",
    "migrate": "deno run --allow-net --allow-read --allow-write --allow-env --env ./src/db/migrate.ts"
  },
  "lint": {
    "rules": {
      "exclude": ["no-import-prefix"]
    }
  }
}